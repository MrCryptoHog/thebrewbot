[build]
builder = "nixpacks"
buildCommand = "pip install playwright && playwright install --with-deps chromium"

[deploy]
startCommand = "python bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
numReplicas = 1

[env]
DATA_DIR = "/data"
